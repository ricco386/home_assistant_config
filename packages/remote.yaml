remote:
  - platform: broadlink
    host: !secret broadlink_ip
    mac: !secret broadlink_mac
    type: rm4_pro

# Broadlink switches with IR remote codes are stored in remotes dir.
# Content of remotes dir is automatically loaded due to packages: !include_dir_named packages in main configuration file.